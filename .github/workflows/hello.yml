name: Hello World!

on:
    push:
        branches:
            -main

jobs:
    hello_world_job:
        name: Hello World Job
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: | 
                echo Hello World!